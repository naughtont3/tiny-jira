endpoint: "https://my.atlassian.net"
user: "myusername@hello.com"
token: "file:/Users/sgrundy/tokens/bogus-testing"
project: "Hello"
# auth: pat   # uncomment for Jira Server PAT (Bearer token); user not needed

# --- Multi-project format ---
# Uncomment below (and remove the flat config above) to use multiple profiles.
# Use -p LABEL to select a profile, e.g.: tiny-jira -p abc issue
#
# projects:
#   infra:
#     endpoint: "https://domain1.atlassian.net"
#     user: "user@example.com"
#     token: "file:/path/to/token1"
#     project: "INFRA"
#   abc:
#     endpoint: "https://domain2.atlassian.net"
#     user: "other@example.com"
#     token: "token-string"
#     project: "ABC"
#   jira-server:
#     endpoint: "https://jira.example.org"
#     token: "file:/path/to/pat"
#     project: "DS"
#     auth: pat    # Bearer token auth; no user needed
#
# default: infra   # optional; falls back to first entry
